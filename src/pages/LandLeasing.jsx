import React,{useMemo,useState} from "react";
import {Shell} from "../components/Site";
import {lands} from "../data";
export default function LandLeasing(){const[q,setQ]=useState(""),[sent,setSent]=useState(null);const list=useMemo(()=>lands.filter(x=>x.location.toLowerCase().includes(q.toLowerCase())),[q]);
return <Shell><section className="page-hero"><div className="container"><span className="eyebrow">FIND PRODUCTIVE LAND</span><h1>Land Leasing</h1><p>Browse farmland and connect with landowners.</p></div></section><section className="section pt-4"><div className="container"><div className="filters land-filters"><input className="form-control" placeholder="Location" value={q} onChange={e=>setQ(e.target.value)}/><select className="form-select"><option>All Land Types</option><option>Dry Land</option><option>Irrigated</option></select><input className="form-control" placeholder="Area (acres)"/><button className="btn btn-success">Search</button></div><div className="row g-4 mt-2">{list.map(l=><div className="col-md-6 col-lg-4" key={l.id}><div className="land-card"><img src={l.image} alt={l.location}/><div className="land-body"><div className="d-flex justify-content-between"><span className="badge-soft">{l.status}</span><span>{l.area} Acres</span></div><h5>{l.location}</h5><p>Andhra Pradesh</p><div className="d-flex justify-content-between align-items-center"><b>₹{l.price.toLocaleString()} <small>/ year</small></b><button className="btn btn-sm btn-success" onClick={()=>setSent(l.id)}>Request Lease</button></div></div></div></div>)}</div>{sent&&<div className="toast-demo">✓ Lease request sent for {lands.find(x=>x.id===sent).location}</div>}</div></section></Shell>}